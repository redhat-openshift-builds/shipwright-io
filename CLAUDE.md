OpenShift Builds (shipwright-io) — downstream packaging repo that wraps upstream Shipwright code via git submodules with Red Hat Konflux build pipelines.

Read @AGENTS.md for architecture, build/test commands, key conventions, and PR conventions.
